spring.application.name=ShopNowBackend
server.port=8080

spring.datasource.url= ${DB_URL}
spring.datasource.username= ${DB_USER}
spring.datasource.password= ${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

jwt.secret = ${JWT_SECERT}

jwt.expiration = ${JWT_EXP}

razorpay.key_id = ${RAZORPAY_ID}

razorpay.key_secret = ${RAZORPAY_SECERT}

spring.security.oauth2.client.registration.google.client-id= ${GOOGLE_ID}
spring.security.oauth2.client.registration.google.client-secret= ${GOOGLE_SECRET}
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8080/login/oauth2/code/google
spring.security.oauth2.client.registration.google.scope=openid,profile,email


spring.data.redis.host= grand-macrosmooth-different-66934.db.redis.io
spring.data.redis.port=17814
spring.data.redis.password=W1jtpz2vpeyPwIWJLEwQBnL81b6JXCPY
spring.cache.type=redis


